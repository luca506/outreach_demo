// Static demo — all API calls use in-memory mock data. No backend required.
export {
  mockGetContacts   as getContacts,
  mockGetContact    as getContact,
  mockUpdateContact as updateContact,
  mockPreviewTemplate as previewTemplate,
  mockGetStats      as getStats,
} from './mockData';
